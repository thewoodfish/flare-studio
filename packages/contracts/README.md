# Contracts

Dependencies are vendored by Foundry but not tracked, so the repo history shows
only our work. After cloning:

```bash
forge install OpenZeppelin/openzeppelin-contracts --no-git
forge install foundry-rs/forge-std --no-git
forge test
```
